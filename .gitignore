# compiled binary (root only, not cmd/charlotte/)
/charlotte

# local docker-compose with host-specific paths (use docker-compose-example.yml as a base)
docker-compose.yml

# runtime data (database and uploads)
data/

# temp directory
tmp/

# editor and OS files
.DS_Store
*.swp
*.swo

# Frontend build artefacts and dependencies
frontend/node_modules/
frontend/dist/

# Claude Code worktrees
.claude/worktrees/

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: charlotte-data
  namespace: charlotte
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: standard   # adjust to match your cluster's storage class
